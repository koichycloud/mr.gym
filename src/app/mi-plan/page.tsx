import MiPlanClient from "./MiPlanClient";

export const metadata = {
  title: "Mi Plan Personalizado | Mr. Gym",
  description: "Portal del Socio para la consulta de rutinas, alimentación, evolución y adherencia.",
};

export default function MiPlanPage() {
  return <MiPlanClient />;
}
